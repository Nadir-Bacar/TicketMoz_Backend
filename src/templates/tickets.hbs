<html lang='pt-BR'>
  <head>
    <meta charset='UTF-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <title>Seus Ingressos para {{eventName}} 🎟️</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        line-height: 1.6;
        color: #333;
      }
      .container {
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
      }
      .ticket-link {
        display: inline-block;
        padding: 10px 20px;
        background: #0066ff;
        color: white;
        text-decoration: none;
        border-radius: 5px;
        margin: 15px 0;
      }
      .footer {
        margin-top: 20px;
        font-size: 12px;
        color: #777;
      }
    </style>
  </head>
  <body>
    <div class='container'>
      <h1>Olá, {{userName}}! 👋</h1>
      <p>Seus ingressos para
        <strong>{{eventName}}</strong>
        estão confirmados! Aqui estão os detalhes:</p>

      {{#if tickets.length}}
        <h2>📌 Seus Ingressos:</h2>
        <ul>
          {{#each tickets}}
            <li>
              <strong>{{this.type}}:</strong>
              <a href='{{this.ticketUrl}}' class='ticket-link'>Acessar Ingresso
                #{{this.id}}</a>
            </li>
          {{/each}}
        </ul>
      {{/if}}

      <p>📍
        <strong>Local:</strong>
        {{eventLocation}}<br />
        📅
        <strong>Data:</strong>
        {{eventDate}}</p>

      <p>Dúvidas? Responda este e-mail ou entre em contato pelo
        <strong>{{supportPhone}}</strong>.</p>

      <div class='footer'>
        Atenciosamente,<br />
        <strong>{{organizationName}}</strong><br />
        <a href='{{websiteUrl}}'>{{websiteUrl}}</a>
        |
        {{socialMediaLinks}}
      </div>
    </div>
  </body>
</html>